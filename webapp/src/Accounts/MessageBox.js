const MessageBox = () => {
        return ( < div >
            <
            div className = "w-25 position-fixed top-50 top-right bg-warning"
            tabIndex = "-1" >
            <
            h6 > I am a pop up message < /h6> < /
            div > <
            /div>)
        }

        export default MessageBox;